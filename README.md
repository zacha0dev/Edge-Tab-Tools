
# Edge Tab Tools (Manifest V3 for Microsoft Edge)

Edge Tab Tools is a small Microsoft Edge extension that enhances tab groups by:
- Showing a full‑width banner with **Group Name - Tab Count - Custom Tab Title**
- Matching the banner and favicon color to the tab group color  
- Letting you **rename tabs** and **rename tab groups** via right‑click  
- Offering a banner ⋯ menu with quick actions and **Hide banner until reload**

This extension uses **Manifest V3**, runs fully locally, and does **not** modify the tab strip or Edge UI.

---

## 🔧 Install in Microsoft Edge (Developer Mode)

Follow these quick steps:

1. **Download the extension ZIP** from GitHub  
   → Go to the repository’s **Releases** section  
   → Download the latest `edge-tab-tools-vX.X.zip`

2. **Unzip** the file to any folder on your computer  
   (must contain `manifest.json` in the root of that folder)

3. Open Microsoft Edge and go to: [edge://extensions](edge://extensions).

4. Turn on **Developer mode** (bottom-left toggle)

5. Click **Load unpacked**

6. Select the **unzipped extension folder**  
(the folder containing `manifest.json`, `content.js`, etc.)

7. The extension loads instantly - refresh your tabs to activate the banner.

---

## 🧪 Using Edge Tab Tools

- Put a tab into a **tab group** → banner appears at the top of the page  
- **Right‑click inside the page** → Rename tab / Rename tab group  
- Click the banner **⋯ menu** → quick actions + Hide banner  
- “Hide banner until reload” hides it temporarily; banner returns on refresh

